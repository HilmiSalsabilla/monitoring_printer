{
  "name": "monitoring-printer-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "Tailwind CSS build for the Monitoring Printer PT. Semen Padang admin views.",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/src/input.css -o ./assets/css/style.css --minify",
    "watch:css": "tailwindcss -i ./assets/css/src/input.css -o ./assets/css/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.13"
  }
}
